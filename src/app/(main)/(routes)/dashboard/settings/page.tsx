import React from "react";
import { SettingsForm } from "@/components/forms/form";

const Settings = () => {
  return (
    <div className="h-screen">
      <SettingsForm/>
    </div>
  );
};

export default Settings;
